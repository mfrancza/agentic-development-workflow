repo_owner           = "your-github-username"
repo_name            = "agentic-development-workflow"
# Include the agent bot identities (e.g. the developer agent) so agents can
# apply agent:* labels (like `agent:review`) and trigger other agents. The
# `[bot]` suffix matches `github.event.sender.login` for GitHub App events.
agent_allowlist = [
  "your-github-username",
  "<developer-agent-app-slug>[bot]",
  "<reviewer-agent-app-slug>[bot]",
]
default_model = "sonnet"

# Auto-trigger configuration. Set a key to true to automatically apply the
# corresponding agent:* label at the natural upstream signal, advancing the
# SDLC pipeline without manual labeling. All keys default to false (opt-in).
# See docs/design/auto-trigger-agents.md for full behavior and security notes.
auto_trigger_agents = {
  groom     = false
  design    = false
  developer = false
  review    = false
}

# GitHub usernames assigned to issues that receive the human-required label
# during grooming (e.g. security-sensitive requests). The grooming agent assigns
# every username in this list. Leave empty to disable auto-assignment; the agent
# will log a warning and skip assignment without failing.
admin_assignees = [
  # "your-github-username",
]

# GitHub usernames requested as PR reviewers when the developer agent determines
# human code review is warranted (e.g. security-related changes). Leave empty to
# disable auto-reviewer-request; the agent will log a warning and skip without failing.
code_reviewers = [
  # "your-github-username",
]
